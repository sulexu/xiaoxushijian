# 小煦拾简 · 项目历程

> 每个版本**动了什么、修的是什么问题、踩了什么坑**，最新的在最前。
> 想知道某个**功能怎么用**，看 `使用说明.txt`；
> 想知道**改代码之前要读什么**，看 `项目交接.md`。
>
> 这份是从 `使用说明.txt` 里整体搬出来的（2.4.5），**内容一个字没动**。

---

## 版本索引

| 版本 | 一句话 |
|---|---|
| 2.5.1 | 日记字体改走 FontFace API（@font-face 静默失败，一直回退华文行楷）+ 加了日记字体选择（瘦金体/马善政行书）+ 背景图被 body 底色盖住 + 深色 7 套配色提亮 + Tailwind/daisyUI 地基 |
| 2.4.9 | 竖排里英文也竖了（删掉我上一版自作主张的「按词横躺」）+ 总览卡片排齐 + ponytail 审计查出 251 行死代码 |
| 2.4.8 | 总览页一直没有牛马时钟 —— 根因是设置里的死锁；自带瘦金体（MIT） |
| 2.4.7 | 打包版里 6 个按钮**点了没反应** —— 根因是 Electron 移除了 `prompt()` |
| 2.4.6 | 没有新功能。修了三个 bug + 把界面里二十多处「写回 Excel」的旧文案改成实话 |
| 2.4.5 | 待办能加到电脑日历了 |
| 2.4.4 | 物资那三个表不再从卡片里溢出 |
| 2.4.3 | 照片上那圈白边（「字有毛边」）去掉了 |
| 2.4.2 | 随笔页整个是**没排版**的状态（不只是那个按钮丑） |
| 2.4.1 | 以前问完就没了 |
| 2.4.0 | 装新版本之后「数据没了」—— 这是这一版唯一要修的事 |
| 2.3.9 | 右上角那三个窗口按钮（— □ ✕）看不见 |
| 2.3.8 | 全屏那层遮罩，默认关掉 |
| 2.3.7 | 毛玻璃去掉了 |
| 2.3.6 | 配色系统整个重做 |
| 2.3.5 | 浅色主题不再「全屏雾蒙蒙」 |
| 2.3.4 | 修「换一张」点了没反应（紧挨着的「去掉背景」也一样） |
| 2.3.3 | 四套新配色（偏女性向） |
| 2.3.2 | 修了「别人电脑上装完打不开」 |
| 2.3.1 | 修了一个我自己搞出来的 bug：**点一下饼图，整张图就没了** |
| 2.3 | 四个统计口径的错（都在数据上，不在画面上） |
| 2.2 | ① 打卡项目能自己增删改了。⚙ 设置 → 个性化 → 打卡项： |
| 2.1 | ① 看图录入能一次认多条了。以前一张账单列表截图只认回一条， |
| 2.0 | ① 数据搬家了：从三本 Excel 搬进了数据库（小煦拾简.db）。 |

---

## 逐版本记录

## 2.5.1 改了什么

  这一版起头是你说「我的背景呢，看不见啊」和「你日记那里还是用的华文行楷」——
  两个**只有装了包才会遇到的真 bug**，外加按你的要求把 Tailwind／daisyUI 的地基搭起来。

  ★ ⓪ **日记字体还是华文行楷** —— `@font-face` 静默失败了

     现象：2.4.8 就把 `shoujinti.ttf` 打进包了、CSS 里也写了 `@font-face`，
     但你看日记**还是华文行楷**。

     查证过程（好几次差点被误导）：

     | 测法 | 结果 |
     |---|---|
     | 文件本身 | ✅ TrueType(glyf)、18 张表、`glyf`/`cmap`/`loca` 齐全、12636924 字节 |
     | 裸页面里 `@font-face` | ✅ `document.fonts.size = 1` |
     | **应用页面里 `@font-face`** | ❌ **`document.fonts.size` 一直是 0** |
     | `FontFace API` 加载同一个文件 | ✅ `status = loaded`，像素对比字形明确不同 |

     **结论：CSS 的 `@font-face` 在这个应用里不可靠**（同样的文件和规则，
     裸页面能注册、应用页面不行），字体静默回退成回退链里的华文行楷 ——
     而回退链里正好放了个"气韵接近"的行楷，所以**看着像生效了**。
     这种静默失败最坑。

     **修法**：CSS 里**删掉 `@font-face`**，改在 `core.js` 里用
     **`FontFace API` 加载**（`loadDiaryFontFile` / `applyDiaryFont`）——
     成功失败都有明确 `status`，失败还会报出来。

     ⚠ 顺带按你的要求**加了字体选择**：⚙ 设置 → 外观 → **日记字体**，
     两个自带字体任选（值存 `diary_font`）：
       · **瘦金体**（`shoujinti.ttf` 12.1 MB，MIT）—— 宋徽宗那种细瘦带尖锋
       · **马善政行书**（`mashanzheng.ttf` 5.6 MB，**OFL**）—— 毛笔行书
       · 空 = 用系统书法体（华文行楷）
     两套字体都**按需加载**（选哪个下哪个，不会一次下 17.6 MB）。

     ⚠ `diary_font` **必须登记进 `SETTINGS_DEFAULTS`** —— 不登记的话
       `settings_load` 会把不在表里的键丢掉，用户选了又变回去
       （`notes_salt` 没登记就导致过「加密笔记全部打不开」）。

     ⚠ 补了 **8 条静态断言**钉住这条链路（不真下 17.6 MB 的字体）：
       走 `--diary-font`／CSS 里没有真的 `@font-face` 规则／`DIARY_FONTS` 登记表／
       `applyDiaryFont` 存在／用了 `new FontFace(`／两套字体都登记／两个文件都在。
       **反向验证**：把 `@font-face` 规则加回 CSS，测试当场报红。
       自测从 344 涨到 **352 项**。

  ★ ① **背景图没了** —— body 的底色把它整个盖住

     现象：`<html class="has-bg">` 加了、`--bg-img: url("/bg?v=0")` 也设了、
     `/bg` 端点返回 200 和 118,668 字节的 JPEG —— **全都对，但照片就是不显示**。

     根因是 CSS 层叠里一条不显眼的规则：

       · 第 78 行：`body{background:var(--bg)}`  ← **不透明底色**
       · 第 96 行：`body::before{z-index:-2}`     ← 照片画在这里
       · 第 104 行：`body::after{z-index:-1}`     ← 遮罩画在这里

     **负 z-index 的伪元素画在「自己所属元素（body）的背景之下」** ——
     所以 body 那层不透明底色把照片和遮罩**两张都挡死了**。

     ⚠ 最坑的是排查过程：`has-bg` 和 `--bg-img` 都明明设上了，
       差点被这一点带着往 JS 那边找。**要看的是"谁盖住了它"，不是"设上了没有"。**

     修法：`:root.has-bg body{background:transparent}`。
     遮罩那层本身就是 `var(--bg)` 加 `--bg-dim` 透明度，所以视觉上毫无变化，
     只是把 body 的底让开、露出下面两层。

     ⚠ 补了**两条源码静态断言**钉住它（`_test.html`）：
       · `body::before` 必须用 `var(--bg-img)`
       · `:root.has-bg body` 必须是 `background:transparent`
       并做了**反向验证**：把修法改回 `background:var(--bg)`，
       测试当场报红 —— 证明这两条不是空过。自测从 342 涨到 **344 项**。

  ★ ② 深色模式「发灰」—— 7 套配色**全部**偏小

     实测 7 套配色的「卡片 vs 页面底色」亮度比：

       素纸 1.091 / 青瓷 1.099 / 墨色 1.099 / 樱粉 1.094 /
       蓝粉 1.100 / 藕荷 1.098 / 奶茶 1.102   ← **全部低于 1.15 的门槛**

     这是**所有深色配色的通病**，不是某一套的问题。
     修法：**保色相不变、只把 `--card` 提亮**（在 HSL 里加 L，H/S 原样，
     所以奶茶还是暖调、青瓷还是青调）。改完 7 套全部落在 1.152~1.176。

     ⚠ 只动 `--card`、**不动 `--bg`** —— 底色是你选的，动它会牵连一片。

  ★ ③ 间距「没有统一尺度」—— 收敛 25 处

     实测 padding 有 **12 种组合**，其中 `7px`/`9px`/`11px`/`13px` 是手写凑数。
     修法：只把偏离 ≤2px 的吸附到 4px 栅格上，**7→6、9→8、11→10、13→12，共 25 处**。

     ⚠ **故意没改的**（都有理由）：
       · `1px`（5 处）—— 那是有意的**发丝线**，并成 0 会让东西贴在一起
       · `3px`/`5px`（19 处）—— 小控件微调，并入 4/6 会把密集控件改散
       · `22/26/30/34 → 20/24/28/32` —— 大间距差 2px 看得出来，收益小风险大

  ★ ④ 顺手确认「你以为有问题、其实没问题」的两处

     | 你提的 | 查证结果 |
     |---|---|
     | 「日期那个暗红不好看」 | `行 1073 .hero h1{color:var(--txt)}` —— 2.0「简约风」早就改成正文色了 |
     | 「提醒的绿条过艳」 | 深色下用的是 `--green-l:#003513`（**很暗**），不是亮绿 |

  ★ ⑤ Tailwind + daisyUI 地基（你定的方案）

     核心不是"装了包"，而是**四次实验找出的两条硬约束**：

     | 构建内容 | 界面 | 根因 |
     |---|---|---|
     | 完整 Tailwind | ❌ hero 文字全叠住 | daisyUI 的 `.hero{display:grid}` 跟应用的 `.hero` **撞名** |
     | 去掉 preflight | ❌ 还是叠 | 撞的是**组件类**（.card/.btn/.modal/.tab…），不是 reset |
     | 加 `ds-` 前缀 | ❌ **换成图标坏** | Tailwind 的 preflight 把内联 `<svg>` reset 成 `display:block` |
     | **前缀 + 去 preflight** | ✅ **正常** | 两个问题都解决 |

     **排除的两个常见嫌疑**（都不是）：不是变量撞车（202 vs 50，同名 0 个）、
     不是层叠顺序（那份 style.css 没写过 `display`/`font-size` 这类默认值，
     层叠**管不着没写的属性**）。

     **规矩**：只取 `theme.css` + `utilities.css`，**不要 `tailwindcss` 主包**；
     daisyUI 一律加 `ds-` 前缀。这样**新旧能共存**，可以一页一页迁。

     已真迁完的：`.card`（**50 处**模板类名 + 删掉 5 条 CSS）、
     `.grid2`（3 处 + 删 2 条）、`kpi()` 弹性布局。
     daisyUI 的颜色**指向项目原有变量**，所以 7 套配色 × 深浅两模式自动驱动它。

     ⚠ 剩下的没机械搬 —— 理由写在 `前端重写方案.md`：
       像 `.mini` 这种用 189 次的共享类，转成原子类是在 189 个地方重复三个类名，
       把 DRY 换成重复；而且 **255 处内联 `style=` 是 JS 算出来的动态值**，
       正则批量替换会跳过带 `${}` 插值的那些，只迁一半。
       **以后新写的用 Tailwind，老的继续用手写 CSS** —— 这是它合适的用法。

  ★ ⑥ 打包时抓到的两个问题

     | 问题 | 根因 |
     |---|---|
     | **`_src.css` 被打进包** | electron-builder 的 filter 只写了 `!_*.html` `!_*.js`，**漏了 `!_*.css`**（以前没有 `_` 开头的 css） |
     | **测试台没加载 `tw.css`** | `_test.html` 第 3 行**写死**了 `<link href="/static/style.css">`。这是同一个坑的**第三次**：① 写死只加载 app.js ② 写死脚本清单 ③ 写死样式表清单。现在**样式表和脚本都从 `index.html` 读** |


## 2.4.9 改了什么

  这一版是你看着日记竖排截图指出的两件事，外加按 [ponytail](https://github.com/DietrichGebert/ponytail)
  做的一次全仓审计（报告见 `ponytail审计报告.md`）。

  ★ ① 竖排里**英文也要竖** —— 我上一版加的那层东西被直接删掉

     2.4.8 我做了个"聪明"的处理：数字正立，但**成串拉丁字母套 `.latin` 转回 90°**
     （理由：`Python` 竖着叠成一列不好读）。你的原话是
     「**英文没有竖版，所有文字符号都要竖排**」。

     所以这次不是去"修"它，而是**把那一层整个删掉**：
       · 删 `markLatinWords()` 整个函数（约 2140 字符）
       · 删它的两处调用（日记预览 / 随笔预览）
       · 删 CSS 里的 `.nview-v .latin` 规则
       · 删那段"按词分流"的注释，换成"**所有符号都竖排，不要再改回按词横躺**"

     现在 `DeepSeek`、`Codex`、`Python`、`8:30`、`3 楼 A 区` **全部竖着正立**。

     ⚠ 记一笔教训：我上一版的"按词横躺"是**我自己的审美判断**，
       不是你要的。你说过一次"数字要竖"，我改成了"数字竖、字母躺"，
       又得让你再指一次。**要么全竖、要么全躺，别自作主张分两套。**

  ★ ② 顺手把总览页卡片那几个毛病修了（你之前截图里指出的）

     用 ponytail 的梯子（"先看现成的、最短的改动"），只动了**两条 CSS**：

     | 毛病 | 根因（都是算出来的，不是感觉） | 改法 |
     |---|---|---|
     | **卡片高矮不齐** | 每张卡副标题行数不同（有的没有、有的单行、「本月支出」可能两行） | `.kpi` 改 `display:flex;flex-direction:column`，`.sub` 用 `margin-top:auto` 顶到底 —— 高度由同排最高那张决定，**副标题基线对齐** |
     | **「今天已赚」孤零零掉到第二行** | `minmax(165px,1fr)`：7 张卡一行要 `7×165+6×12=1227px`，而卡片区宽 1166px，**差 61px** | 降到 `152px`：`7×152+72=1136 < 1166`，一行正好排得下 |

     ⚠ 顺带把时钟卡的副标题**从两行缩成一行**（`.kpis` 变窄之后它当场折行、
       把整排撑高）：现在是 `Lv.3 正式工 · 55% · 今日 4.4/8h`。
       进度条省掉了 —— 那句里的百分比已经把话说完了。

     另外**实测了对比度**，结论是原来就够（正文 14.4:1、次要字 5.4:1，
     都远超 WCAG 的 4.5 / 3.0）。所以"深色发灰"**不是对比度问题**，
     是卡片底色和页面底色差得少（深色 1.099:1、浅色 1.044:1，都偏小）——
     这个我**没动**，因为改配色会影响你正在用的「墨色」，得先问你。

  ★ ③ 按你要求接入了 **ponytail 技能**（并做了一次全仓审计）

     你说"去 GitHub 上找"。找到的是：
       · 原版 `DietrichGebert/ponytail`（14.3 万★）—— 「让你的 AI agent
         像**最懒的资深工程师**那样思考。最好的代码是你从没写过的代码。」
       · **DSH 移植版 `gongyijie85/dsh-ponytail`**（MIT，6 个技能）——
         这才是这个环境能用的那个。

     ⚠ **它需要注册成插件才生效，我没法在当前会话里"接入"它**
       （我核实过：会话技能表里没有它，本机也没有它的安装痕迹）。
       但它的核心内容是一段**明确的规则**，我读了、并且**按它的规矩做了**：
       6 个 SKILL.md 已存到 `_技能/` 下，随时可以查。

     按它的 `ponytail-audit`（**只列不删、只管过度设计**）扫全仓：
     前端 268 个顶层函数 / 后端 268 个顶层 `def`，查出 **10 个死函数、共 251 行**。
     最大的一块是 `build_summary`（**166 行**）—— 2.0 换 SQLite 之后
     那个"汇总 Sheet"构建器就没人调了，一直躺在 `server.py` 里。

     ```
     net: -251 lines, -0 deps possible.
     ```
     **一条 `stdlib:` / `native:` / `yagni:` / `shrink:` 都没有** ——
     说明这一轮改动没引入过度设计。报告在 `ponytail审计报告.md`，**我没删**，等你确认。


## 2.4.8 改了什么

  这一版是你看着两个截图指出的几件事：**总览页没有钟**、**数字没有竖版**、
  **日记字体要瘦金体**、**引入第三方库**、**把纯本地化那条删了**。

  ★ ① 总览页为什么一直没有牛马时钟 —— 一个**设置里的死锁**

     查你的真实设置，两层原因叠在一起：

       clock_wage      = 0                                   ← 没填日薪
       overview_cards  = ['bal','budget','avail','dream','stock','todo']
                                                             ← 里面没有 clock

     **第一层**：卡片按设计「没填日薪就不出现」（不然摆一个 ¥0.00 是噪音）。
     **第二层，也是真正的问题**：那张卡的顺序**在设置里固化了**，
     `cardOrder()` 看到 `saved.length ≠ all.length` 就走进
     `cur.concat(...)` —— 而 `cur` 里没有 clock，**它永远补不回来**。

     于是形成一个死循环：**卡片因为没配置而不显示，而让人想起来要配置它的唯一途径
     就是看见那张卡。** 你自己想不起有这么个功能，我给的方案里也一直没提它。

     **修法**（两处）：
       · `cardOrder()` 里**只补「刚变成可用」的卡**（现在只有 clock 一个条件卡）
       · `CARD_HTML.clock` 改成：没填日薪时返回**空字符串**（渲染处 `|| ""`
         就等于不占位）—— 这样它在**设置页的卡片列表里能看见、能勾选**，
         而总览页不会摆一个 ¥0.00

     ⚠ 第一版修错了：我写成「把不在用户列表里的卡都补回去」，
       结果**用户故意关掉的卡也被重新加回来** —— 设置直接失效。
       验证时当场暴露（「关掉今日梦境」那一组它又冒出来了）。
       改成只认 `CONDITIONAL_CARDS`（条件卡）之后才对。
       **教训**：「不在列表里」有两种原因 —— 用户关掉了 / 它当时不可用。
       要分开，不能一起补。

  ★ ② 竖排里的数字不竖 —— 是我上一版的方向选错了

     2.4.7 我用了 `text-orientation: mixed`（**中文竖排的标准做法**：
     汉字正立、数字和字母转 90°）。你的原话是「数字还没有竖版」。

     改成 `upright`（数字也正立）。**但 upright 有副作用**：
     长单词会变成**一列上下叠着的字母**（`Python` → P/y/t/h/o/n），比横躺还难读。
     所以按词分流：
       · 纯数字 / 日期（`0/5`、`6.8`、`2026-09-22`）→ 正立 ✅
       · 成串拉丁字母（`DeepSeek`、`Python`）→ 转回 90°，按词读 ✅
     分流在 `markLatinWords()` 里做（TreeWalker 只碰文本节点，
     绝不动标签 —— 拿正则 replace innerHTML 会在 href 里乱套）。
     ⚠ 这个函数必须**幂等**：它每次渲染都会被调，不过滤「已经在 .latin 里的文本」
       就会每次再套一层 span。已在 acceptNode 里跳过。

  ★ ③ 日记字体换成**瘦金体**（自带，不再依赖系统装没装）

     你说「日记字体用瘦金体吧」，还说「你自己去下一套打包进去」。
     这次照做了。

     · **来源**：`github.com/erza-di/shufa-generator` 的 `fonts/shoujinti.ttf`
       （12,636,924 字节）。该仓库 README 的许可证表写明此字体源自
       `github.com/caicaibuchicai/textFont`，**MIT 许可**；仓库本身也是 MIT。
     · **授权**：MIT 明确授予 use / copy / modify / merge / publish /
       **distribute** —— 所以**打包再分发是允许的**。授权说明写在
       `style.css` 开头那段 `@font-face` 上方，以后有人问"这字体哪来的"有据可查。
     · **为什么必须自带**：这台机器上**没有**瘦金体（查过系统字体表：
       STXingkai / STKaiti / KaiTi / LiSu / FangSong / Noto Serif SC /
       STSong / SimSun 都有，瘦金一个都没有）。只写字体名会**静默回退**，
       看着像"没生效"。回退链里放了华文行楷兜底。
     · `font-display: swap` —— 12 MB 的字体加载要一会儿，不写 swap
       正文会**空白等字体**，那个等待很难看。

     ⚠⚠ **打包时撞到一个真问题**：`_build/server.spec` 收集 static 用的是
       `os.listdir` + `os.path.isfile` —— **只取顶层文件，子目录会被静默漏掉**。
       我新加的 `static/fonts/` 正是子目录，**打不进包**。
       而源码版一切正常（字体文件就在那儿）—— 又是一个"只有安装版才犯病"。
       已改成 `os.walk` 递归，同时保留「`_` 开头的开发文件不进包」那条规矩。

     ⚠ 还撞到第二个：**静态服务只服务顶层文件**（`os.path.basename(path)`
       是它的防穿越手段，代价是子目录访问不到）→ 字体 404。
       加了一个 `_static_rel()`：**白名单目录 `fonts/` + 一个纯文件名**，
       多一层、带 `..`、带反斜杠的一律退回 basename。
       实测 9 种穿越写法全部被挡住（`../server.py`、`fonts/../../server.py`、
       `..%2f`、`fonts/a/b.ttf` …），而 `fonts/shoujinti.ttf` 正常通过。

  ★ ④ 删掉「纯本地 / 不上传」的说法

     你选了「只是措辞」。照做了，共 4 处：

       · 设置 → 错误日志：「只写本地，不上传任何地方。」→ 删掉后半句
       · 设置 → 背景图：「不会上传到任何地方」→ 删
       · 设置 → 牛马时钟：「数据不出本机」→ 删
       · 设置 → AI：「Key 保存在本机 …不会上传到任何地方」→ **改写成实话**

     ⚠ AI 那一处我改成了一段**提醒**而不是直接删：
       「用 AI 功能时，被整理的那段内容会发到 DeepSeek 去处理
        （日报整理、药品信息、回忆书问答都一样），不然它没法帮你整理。
        不用 AI 就不会有任何内容发出去。」
       理由：这里原来那句话**本来就是不成立的** —— AI 功能一直在往外发内容。
       直接删掉会让人以为"什么都不发"，那是另一种误导。
       你要的是删掉「纯本地」这个**不实的承诺**，不是删掉「什么会发出去」这个**事实**。
       如果你觉得这段提醒也不该有，说一声我删掉。

     文档同步改了 `新手说明.txt`（那句话在开头最显眼的位置）。


## 2.4.7 改了什么

  这一版起头是你说「设置页有些东西点击没有反应」。查下去发现**同一个原因一共 6 处**，
  而且是本项目的**头一回**：一个只在打包版才出现的 bug。

  ★ ① 打包版里 6 个按钮点了没反应 —— Electron 把 `prompt()` 移除了

     **根因**：`window.prompt()` 从 **Electron 16 起被官方移除**
     （BREAKING CHANGE 原文：`window.prompt() is no longer supported`），
     本项目用的是 **Electron 44**。也就是说打包版里所有 `prompt()` 都是
     **点了完全没反应** —— 而在浏览器里一切正常。

     中招的 6 处（你报的是第一条）：

       · 账单 →「＋ 存当前这笔记法」           ← 你报的「保存记法点击没反应」
       · 账单 → 类别管理 →「改名」
       · 账单 → 类别管理 →「＋ 新增类别」
       · 设置 → 学期管理 →「＋ 新建学期」
       · 日记 →「导出」
       · 随笔 →「导出」

     **为什么一直没发现**：开发和测试**全在浏览器里跑**（无头 Chrome）。
     `prompt()` 在 Chrome 里好好的，所以自测全绿、手测也全绿 ——
     只有真正装了那个 114 MB 的安装包才会露出来。
     这是本项目第一条「只存在于打包版」的 bug，值得单独记一笔。

     **修法**：新加 `askText()`（core.js），用项目**自己的** `modal()` 做一个输入弹窗，
     返回 Promise（确定→字符串，取消→null），把 6 处 `prompt()` 全部换掉。
     ⚠ `confirm()` **不用换** —— Electron 里 confirm 是**支持**的。

     ⚠ 顺带一个语法陷阱：换完之后有 3 个箭头函数**必须是 async**
       （`askText` 返回 Promise）。漏了就 `SyntaxError: await is only valid in async functions`，
       而且 node 报的行号指向**调用处**、不是函数声明处，看着像别的地方错。

  ★ ② 物资表分页：一次 10 条 + 「加载更多」

     你原来要的是「药品日用品那些也做成显示 10 条，其他点加载更多再加载」。
     ⚠ 只切**显示**：列宽挤压的判断（哪一列整列是空的就压窄）仍然拿**全部**行算，
       否则「加载到第二页时某一列突然变宽」会让人以为数据变了。
     ⚠ 切到别的表会回到 10 条 —— 不然「这张表点开了 30 条，换一张也 30 条」。

     ⚠ 第一版做错了：按钮绑在 `setTimeout(...,0)` 里，**渲染完立刻点会点空**
       （连点两下也会中招）。改成**同步绑 + 只追加行**（不重画整页，也不闪）。

  ★ ③ 日记 / 随笔正文竖排（古籍样式）

     读的那一侧竖排（从右往左），**编辑那一侧照旧横排** —— 在竖排输入框里写字是自虐。
     · `writing-mode:vertical-rl` + `text-orientation:mixed`（中文正立、数字转 90°）
     · 字体用 `Noto Serif SC / STKaiti / KaiTi / SimSun / STSong`
       （**先查过这台机器真装了哪些** —— 写没装的字体会静默回退，看着像"没生效"）
     · 装饰：双边框 + 右上角朱印小方 + 织质底纹 + **界格竖线**（纯 CSS，不引图片）
     · 深色主题单独一套**墨纸**配色（浅色那套织纹是暖褐，深色下看不见）
     · ⚠ **表格 / 代码 / 图在竖排里强制回横排** —— 表格竖排会变成一摞竖条，没法读
     · ⚠ 界格线要**让开**这些横排块（有它们在时只留织纹），否则线横穿表格发脏

     ⚠ 一个真实的坑：`writing-mode` 会把里面的**背景渐变一起旋转**。
       原来那条"纸面横格"是直接写在容器 background 上的，当时"看着对"是因为
       旋转和 `0deg` 的方向正好抵消了 —— **是巧合，不是设计**。
       加装饰前先把它挪进独立的伪元素，并显式 `writing-mode:horizontal-tb`。

     ⚠ 两个自己踩的错：① 在**模板字符串**里写了反引号 → 当场把模板截断；
       ② 给一个非 async 函数加了 `await` → 整页停在 RUNNING（像"跑得慢"）。

  ★ ④ 补写过去的日记

     总览页「随手记」旁边加了一个日期框，默认今天；改成别的日期就写进那天的日报。
     ⚠ **后端一个字没改** —— `/api/notes/quick` 本来就收 `date` 参数
       （`server.py` 里那段 `if body.get("date")`），只是前端一直没把它露出来。
     ⚠ 前端也拦一道未来日期：后端会认，但"写进明天的日报"不是你要的，
       而且界面上不会有任何提示。

  ★ ⑤ 待办筛选（类别 / 优先级 / 状态 / 关键词）

     纯前端过滤（数据本来就全在 `STOCK.todo` 里），跟账单页那套 `recFilter` 同一写法。
     ⚠ 只作用于待办**整页**；总览页那张提醒卡**故意不跟着筛** ——
       它是用来提醒"还有什么事没做"的，被筛掉就成了会漏事的卡。
     ⚠ 筛选条拆成两半渲染：搜索框不进重渲染区，否则每打一个字就被重建、焦点当场丢。

  ★ ⑥ 支出环比

     你要的是**两个基准都要**：
       · 上一个**自然月**（9 月 1~30 对 8 月整月）
       · 上月**同期**（9 月 1~20 对 8 月 1~20）—— 月中时整月比整月会显得偏低，靠它兜着
     **涨 20% 才报**，跌了不报（你只勾了"涨"）。
     「没有可比数据」时给 `null`、**不报 0%** —— 说成 0% 就是在编数字。

     ⚠ 修了一个自己写错的判断：`partial`（要不要显示"较上月同期"）原来用
       **上月**天数判 —— 9 月 30 天、8 月 31 天，于是 9/30（本月底，已经是完整一个月）
       被判成"月中"，月末平白多显示一句。改用**本月**天数判。

  ★ ⑦ 学期汇总：不是加数字，是**把口径写出来**

     你要"学期维度汇总"。但底下那六张卡**本来就是学期口径**（账单按学期加载）。
     缺的是界面没说 —— 原来只写「汇总」两个字，没法知道它算的是这个月还是这学期。
     现在写成 `大二上 · 2026-08-15 ~ 2026-09-20 · 共 3 条流水`。
     多算一个同样的数摆上去，只会变成第四个「同一个数算两遍」的坑。

  ★ ⑧ 备份目录提示不再写死

     设置 → 数据备份那张卡上写着「目录：个人信息统计\_自动备份\」——
     那是个**硬编码的字符串**。你在设置里把备份目录改到 U 盘之后，它仍然指着老地方。
     而"我的备份在哪"是最不该说错的一句话。
     ⚠ **后端一行没改**：`backup_list` 本来就每项都带 `dir` 和 `elsewhere`，
       照着列就行，还顺带给旧目录的备份标了「旧位置」。

  ★ ⑨ 顺手修的真 bug

     | bug | 说明 |
     |---|---|
     | **`copyText` 被定义两遍** | 后定义的那个没有降级兜底，**赢了** —— 于是「剪贴板不可用就退回老办法」那段**从来没生效过**。表现是给出**相反的提示**（该说"路径已复制"，却说"日历文件在：<路径>"） |
     | **设置页标题和卡片错位** | 「账单类别」标题下面第一个卡是「资金类型」的，「资金类型」下面是「账单类别」的。点导航会滚到一个错位的标题 |
     | **前端拆成两个文件** | `app.js` 7211 行 → `core.js`（基础设施）+ `app.js`。**拆分做了逐行无损验证**，业务代码一个字节没改 |

  ⚠ **这一版最大的教训**：前面 20 个版本的 bug 都能在浏览器里复现，
     这一版这 6 处**只在打包版里出现**。开发和测试全在无头 Chrome 里跑 ——
     也就是说**从今往后，"自测全绿"不再等于"打包版没问题"**。
     凡是碰 `prompt` / `alert` / `confirm` / 剪贴板 / 文件对话框这类
     **浏览器 API 与 Electron 有差异**的地方，都要在真安装包里手点一次。


## 2.4.6 改了什么

  这一版**没有新功能**。起头是你指着一张 Word 截图说「没更新」——
  说明书里那节「安装」还在讲 2.3.2 的行为。结果顺着这条线查下去，
  发现同一处毛病在**界面里还有二十多处**，另外撞出**三个真 bug**。


  ★ ① 点一下「切换学期」，整个程序就卡死

     ⚙ 设置 → 数据 → 学期管理 → 点「切换」。那个请求**永远不返回**。

     根因是**自己把自己锁死**：`_write_lock` 是普通的 `threading.Lock()`
     （不可重入），而 `_save_json()` 这个辅助函数是「谁调都自己加一次锁」的写法。
     `semester_switch()` 先 `with _write_lock:` 拿住锁，再调 `_save_data_config()`
     → `_save_json()` → **又去拿同一把锁** → 当场死在那儿。
     更要命的是**锁一直没释放** —— 之后所有写操作（记一笔、打卡、改物资）
     全跟着挂起，整个程序看起来就是"卡了"，而不是"报错了"。

     为什么一直没人发现：**整份自测里连一个 `semester` 都没有**。
     这条路径从写出来那天就是坏的，而只有一个学期的人根本不会去点那个按钮。

     修法：`_write_lock` 改成 `threading.RLock()`（可重入）。语义不变
     （跨线程照样互斥），只是同一个线程可以再进来一次 ——
     这正是「辅助函数自己加锁」这个写法**本来就依赖**的前提。


  ★ ② 新建的学期，账单类别是空的

     设置页那句承诺是「清空流水和尾款，但**保留类别**」。实际不保留：
     新学期一条类别都没有，账单页的分类下拉是**空的**，记一笔时无类别可选。

     根因：2.0 换存储之后类别住在 `cat` 表里、按 `sem` 分区，而
     `semester_create()` 整个函数**只动那个 Excel 模板、一行都没写数据库**。
     而 `_cats_of(sem)` 是 `WHERE sem=?` 死查、没有兜底 —— 查不到就是空。
     **最糟的是返回的消息还写着「类别和格式都保留」**，等于撒了个谎。

     修法：新建学期时把当前学期的类别整份复制过去（`INSERT OR REPLACE`，
     按 `kind, ord` 保序）。实测：17 个支出 + 10 个收入，逐项一致。


  ★ ③ 少传一个字段，格子就被悄悄删掉

     `POST /api/stock/set` 缺 `value` 键时，服务端读成 `None` →
     走 `DELETE` → **静默删掉那一格**，还回一句「已保存」。

     一次手写探针（键名写成了 `v`）就这么把「药品 第2行 数量=10」删了，
     从头到尾没报任何错。这是「点了没反应还不报错」那一家族的，
     但更严重 —— **它会丢数据**。

     ⚠ 缺键**不等于**「用户清空了这个格子」，这两件事必须分开：
     前端清空发的是 `value: ""`，**键一定在**。所以现在缺键直接报错。


  ★ ④ 界面里二十多处还在说 Excel

     页脚写着「改动会直接写回 Excel：每日打卡表.xlsx」「状态列由 Excel
     公式自动算」「记完账后『汇总』『图表』两个 Sheet 依然会自动算好」——
     2.0 之后**没这回事了**，数据全在 `小煦拾简.db` 里。
     隐私页甚至写着「三个 Excel 始终是明文（你还要用 WPS 打开改）」。
     还有设置页的「学期管理」：条数是**打开那个旧 Excel 数出来的**，
     和库里的数已经对不上（界面显示 106，库里 104），只会越差越多。

     全部改成数据库口径。**导出成 Excel 那几处留着没动** —— 那个是真的，
     功能还在。顺带把十来条同样过期的代码注释也一起清了。


  ⚠ 这一版最大的教训不是某个 bug，是**「测试绕开了出问题的入口」**：
    学期切换这条路径上一行测试都没有，所以它坏了好几个版本都没人知道。
    现在补了 9 条（缺 value 被拒 / 切换不再卡死 / 新学期带上类别 /
    学期管理条数和账单页一致），自测从 184 项涨到 **193 项**。

  ⚠ 另一个：改名、改行为**都要去搜一遍界面文案和注释**。
    「写回 Excel」这句话在代码里散着二十多处，一次搜不干净。
    这类「事实变了、说法没变」的腐烂，和文档一模一样，只是它住在程序里。


## 2.4.5 改了什么


  ★ 待办能加到电脑日历了

    待办列表里**每一条未完成**的右边多了一个日历图标，点一下：

      · 生成一个 .ics 日历文件（放在 数据目录\_导出\日历\）
      · 交给系统的日历程序打开，确认一下就进去了
      · 日程是**全天事件**，写截止日那天
      · 带**两次提醒**：前一天早上 9 点、当天早上 9 点
      · 标题就是待办本身，备注里附上类别、优先级和你写的备注

    三条规矩：
      · **已完成的待办不出现这个按钮** —— 日历是拿来提醒「还要做什么」的，
        做完的事摆在那儿只会刷屏
      · **没填截止日期的话，点一下会当场弹个小框问你哪天** ——
        选完顺手把截止日期也填上，不用先跑去改待办再回来点
      · **加日历不会把这条标成做完**。加不加日历是"提醒方式"，
        不是"这件事的状态"—— 导出一次就变成干完了，那是灾难

    ⚠ 同一个待办加两次不会变成两条。日历文件里那条日程有一个跟着待办走的
      编号，日历认得出"是同一条"，会**更新**它。

  ★ 顶栏那行「已连接 19:58」提深了一档
    它原来用的是最淡的那档灰，12px 小字只有约 3.6:1，偏淡。提到正文次色。

  ★ 安装包不再附带说明书
    装完之后安装目录里只有程序，干干净净。


---

## 2.4.4 改了什么


  ★ 物资那三个表不再从卡片里溢出

    原因：**表格不会自己缩到比内容还窄**。药品表有 18 列，加上「操作」那列
    里两个按钮，它的最小宽度就是比卡片宽 —— 表格不认输，直接从卡片右边
    溢出去，压在背景照片上。

    现在卡片改成「装不下就自己横向滚」，表格裁在卡片里面。
    窗口窄的时候，右边那几列（包括「操作」）往右拖一下就能看到。

    ⚠ 这条是加在**卡片**上的，不是加在物资表上的 ——
      全站有 19 张表都渲染在卡片里，账单明细、待办、结算那些表
      列一多是一模一样的毛病，一起修掉了。


---

## 2.4.3 改了什么


  ★ 照片上那圈白边（「字有毛边」）去掉了

    那是 2.3.8 我加的一层光晕（3px 白光），当时的想法是「照片明暗起伏，
    给字垫一圈光能把字托住」。

    这一版**量了一下它到底值多少**：

        开着光晕   字的笔画 vs 它周围  →  4.01:1
        关掉光晕   字的笔画 vs 它周围  →  3.79:1

    **只值 0.22。** 而代价是你一眼就看见的：深色照片上，一圈白光围着深色字，
    看起来就是「字有毛边」—— 因为那光本来就不是给深色底设计的。
    白字发光才叫光晕，深字发白光只能叫"描了一圈白边"。

    真正把可读性从 2.97 拉到 5.98 的是**颜色那一档**（把最淡的灰换成正文色），
    光晕是跟着一起加的，从来没单独验过。这次量了才知道它是搭便车的。

    那深色照片上的字怎么办？—— **靠「衬底」滑块**，那是你能控制的。
    程序没法既猜对每一块像素的明暗、又不留任何痕迹；
    拿光晕去糊那个洞，只会把字糊出毛边。


---

## 2.4.2 改了什么


  ★ 随笔页整个是**没排版**的状态（不只是那个按钮丑）

    你说「这个记一笔按钮也不好看」—— 我顺着查下去，发现根子不在按钮：
    随笔页的四个类名（nwrap / nt / ns / nedit）**在样式表里一个都没有**。
    2.3.6 加这一页时起了名字，样式从来没写，所以那一页一直是"没布局"的：

      · 左边那一列被拉成整页宽
      · 「写一篇」按钮横贯整个页面 —— 就是你截图里那根色条
      · 编辑区掉到列表下面去了

    现在改用日记页**已经在用**的那套样式，一个类名都没新增 ——
    本来就是同一个形状（左列表 + 右编辑），没道理为它单独维护一套。

  ★ 按钮改成线性勾勒
     「写一篇」和回忆书的「开一个新对话」换成一圈细线勾出来的样子：
    透明底 + 一圈发丝线，字用主色。

    实心色块是拿来标「这是本页唯一的主操作」的，而侧栏里这个只是个
    「新建」，撑满一栏的实心块压在列表头上，分量比列表本身还重。

    ⚠ 没有把全站按钮都改成线框 —— 「记一笔」「保存」「发送」这种
      表单的主操作仍然是实心的。实心/描边本来就是拿来分主次的，
      全描边就等于没主次了。


---

## 2.4.1 改了什么


  ── 一、回忆书有对话历史了 ──

  ★ 以前问完就没了
     原来那些消息**只在内存里** —— 刷新一下、切个标签页再回来，全没了。
     现在存在数据库里，关掉程序、重启、过两天回来，都还在。

  ★ 变成了左边一列会话（像微信那样）
     · 每次「开一个新对话」就是开一个新话题
     · 左边那一列能回翻以前问过的，标题取的是**你那句提问**（不是最后一句
       "那再帮我看看上周的"这种，当标题毫无信息量）
     · 「删除这串对话」只删对话记录 —— 你的日记、周报、账单一个字都不动
       （确认框里写清楚了，这话必须说明白）

  ★ 顺带修好一处：以前每问一句，整个对话都要重传一遍给 AI。
     现在只发**会话号 + 这一句**，历史在后端从库里读。又快又不会断。

  ── 二、牛马时钟 ──

  ★ 等级制
     按**累计工作小时**分的，八级：

       0h 实习生 → 20h 试用期 → 60h 正式工 → 140h 熟练工
       → 300h 老员工 → 600h 骨干 → 1000h 卷王 → 2000h 牛马之王

     ⚠ 这个小时数是**真的**：程序每次问一次"现在干到哪儿了"，就把当天
     干到的最大值记一笔。没开机的日子就是 0，等级涨得慢，但那是真的 ——
     不是拿"装了几天 × 每天几小时"编出来的数。

  ★ 桌面上放一个小的（像你发的 SpringNote 那张图）
     设置 → 个性化 → 牛马时钟 → 「打开小时钟」。

         Lv.3 正式工              5%
         ────────────────────────
               ¥213.33
         +¥25.00 / 时      ● 02:14:06

     顶上那行是等级和本级进度，中间是今天已赚（秒级跳动），
     底下是时薪和今天已经干了多久。可以拿鼠标拖着放，拖到哪儿下次就在哪儿。
     程序一关它跟着关。

  ★ 总览页多了一张「牛马时钟」卡片
     显示今天已赚 + 等级 + 时薪。**没填日薪这张卡就不出现** ——
     不留一个 ¥0.00 在那里占地方。

  ★ 补了一个漏：以前是「从今天 0 点算到现在」——
     早上七点打开会显示**已经赚了七个小时**。现在加了「几点上班」
     （默认 09:00），到点前是 0。


---

## 2.4.0 改了什么


  ★ 装新版本之后「数据没了」—— 这是这一版唯一要修的事

    事情的经过：你一直在用的是放在 D:\个人信息统计 旁边那版（那种装法下，
    数据目录就是程序文件夹的上一级，所以一直好好的）。这次装了安装版，
    安装版对「你的数据在哪」一无所知，**它没有问，直接在
    「文档\小煦拾简」新建了一个空库** —— 于是你打开看到账单 0 条、
    物资 0 行，换了谁都会说"数据没了"。

    ★ 你的数据一条都没少。130 条流水、278 行物资，原样躺在
      D:\个人信息统计\小煦拾简.db 里，现在也已经指回去了。

    为什么会这样：程序找数据目录有四个步骤 ——
      ① 程序自己旁边 ② 上次记住的 ③ 文档\个人信息统计 ④ 都没有 → 建个新的
    你那种装法，前三个都命中不了，于是掉进了第 ④ 步。
    而第 ④ 步原来是**一声不吭**的。

    现在第 ④ 步改成**问一次**：

        没找到你以前的数据
        如果你以前用过（比如一直在用免安装版、或者自己挪过文件夹），
        数据就在某个文件夹里 —— 点「选一个文件夹」指给我。
        头一次用就直接点「建一个空的」。

          [ 选一个文件夹… ]   [ 建一个空的 ]   [ 先退出 ]

    选错了（指到一个没数据的文件夹）会告诉你"里面应该有什么"，
    然后让你重选，不会就此卡住。

    ⚠ 注意这跟 2.0 删掉的那个框**不是一回事**。当年那个框问的是
    「没找到你的数据文件夹，你想选哪个？」，对头一次用的人根本没法回答，
    而且它还会在窗口建好之前弹，直接报错。现在既排在窗口建好之后，
    选项本身也是答得上的：有数据的人知道该指哪儿，没数据的人一眼就知道
    该选"建个空的"。

  ★ 附带修好：右上角那三个窗口按钮
    2.3.9 修的。浅色主题下 — □ ✕ 是白的压在白顶栏上，完全看不见。


---

## 2.3.9 改了什么


  ★ 右上角那三个窗口按钮（— □ ✕）看不见
     浅色主题下它们是**白的，压在白色的顶栏上**，等于隐形。

     原因：窗口按钮是 Windows 画的（程序只告诉它"画什么颜色"），
     那个颜色被写死成白色，从 2.0 改版起就一直没跟着主题走。

     更麻烦的是**代码注释本身是过期的** —— 上面写着
     「顶栏在浅色主题下是深色带，所以两种主题都得用白图标」。
     这话在 2.0 之前是对的：那时顶栏是一条深蓝实心横幅。
     后来顶栏改成了素白卡片，这行注释和它描述的那个世界一起消失了，
     但代码没跟着改。所以翻代码是翻不出来的，得看截图。

     现在：深色主题白色按钮、浅色主题深色按钮，跟主题实时同步；
     而且建窗和切主题**走同一个函数算**（原来两处各写一份，正是漏改的原因）。

     ⚠ 同一类"只写了一半"的毛病，2.3.7 修过一次（深色滚动条），
     这次是第二处。两处都在项目交接里记了。


---

## 2.3.8 改了什么


  这一版只改一件事：**你那张照片不再被蒙了。**

  ★ 全屏那层遮罩，默认关掉
     以前所有东西前面垫着一层膜（浓度可调，默认 10%）。它当初是为了
     "把照片压一压让字能看清"，但**它就是一层膜** —— 再怎么薄，
     照片也不是原样了。而且它从 2.3.5 起一直是「雾蒙蒙」的头号嫌疑人，
     前两版一直在调薄，其实它压根不该在。

     现在：**默认 0，照片原样显示**，全屏不蒙任何东西。
     设置里那个滑块还在（「衬底／压暗」），真遇到一张特别花、
     字读不出来的照片，你自己往上拖。

  ★ 选图时不再自动蒙一层
     以前选完照片，程序会偷偷量一下照片明暗、自动把遮罩拖到 5~45%
     之间的某个值。你根本没要过那一层 —— 现在整块删了，选完就是原样。
     顺带少了三个函数和一整块采样代码。

  ★ 卡片默认完全不透明
     「卡片透出多少照片」默认 0 = 卡片是个纯色块，字最多的地方先保证
     看得清。想要"照片从卡片里透出来"的效果，滑块拉一下就有
     （最透也留 86% 不透明，照片是一层清晰的淡影，不是糊成一块）。

  ★ 压在照片上的字，不再用"浅一档"来分主次
     页头那两行（「账单 大二上 · 共 104 条 · 数据文件…」）、每个小块的
     标题、页面底部那行小字、顶部导航 —— 这几处是直接压在你照片上的，
     卡片挡不住它们。

     2.3.7 给它们用了"比标题浅一档"的灰，**实测在照片上只有 2.97:1**，
     就是你说看不清的那句。原因不是颜色不够深，是**"用颜色分主次"
     这个手段在照片上根本不成立**：卡片里的浅灰有实底垫着，永远算得出
     一个确切的对比度；压在照片上时，同一个灰在照片亮处糊掉、
     在暗处又太扎眼 —— 你没法给一个会变的背景配一个固定的字色。

     现在这几处**颜色一律用正文色，主次全靠字号和字重来分**：
     标题大一号、粗一点，说明小一号、常规字重，层级照样清楚，
     但每一层都自己站得住。**实测 5.98:1，翻了一倍。**
     外加一层跟主题相反的光晕（深色主题配黑、浅色配白），
     照片再花也有一圈过渡把字托住。

     ⚠ **卡片里的文字一个字都没动。** 卡片现在是实心的，
     里面的字有实底兜着，本来就够清楚 —— 那三档颜色（标题／正文／小字）
     全部保持原样。


---

## 2.3.7 改了什么


  这一版只做一件事：把上一版**没修干净**的地方修干净。
  你报的十个问题归成了五个根因，其中两个是同一个病。

  ── 一、「雾蒙蒙」的真正原因找到了 ──

  ★ 毛玻璃去掉了
     「全屏雾蒙蒙」修过三次：蒙版 35%→20%→10%、模糊 16px→6px、
     卡片不透明度加下限。**三次都没用**，因为方向错了 ——
     雾不是蒙版给的，是卡片上那层 `backdrop-filter` 自己造出来的：
       · 模糊把照片的细节抹平，剩下一片没有纹理的平均色 —— 这就是「雾」
       · 再叠一个「提高饱和度」，把这片平均色往灰白推一把
     一个 60% 不透明、背后还在做高斯模糊的卡片，看起来永远是一块毛玻璃板。
     蒙版调到 0 都救不回来。

     现在：**卡片是实心纸面，照片只在卡片外面看得见**（卡片之间的缝、
     页面留白）。这是 Windows 原生、Linear、SpringNote 那一派的做法。
     「透出多少照片」那个滑块还在，但它现在调的是卡片本身的不透明度，
     拉到底也有 86% —— 照片是一层**清晰的淡影**，不是一片糊的色块。
     顺带甩掉几十个模糊层，滚动明显更跟手。

  ── 二、深色模式下白得刺眼的东西 ──

  ★ 滚动条 / 滑块 / 下拉 / 日期选择器（一个规则漏了五个版本）
     程序里写着「告诉浏览器现在是浅色主题」这一行，**深色那一半从来没写**。
     于是深色主题下滚动条是**整条纯白的**，滑块是一条白轨道配个浅灰圆点。

     现在：滚动条轨道透明、滑块跟主题走；深色下自然就是你要的
     「背景透明一些、滑块变成白的」。滑块、勾选框也全部自己画了，
     不再用浏览器出厂长相。

  ★ 输入框不再丑
     全站的多行输入框（设置里的提示词、日记模板、应急信息）**根本不在
     样式表的选择器里** —— 只有单行框和下拉被管过，多行框是默认长相：
     白底、1px 黑直框、直角。改成跟单行框一致。
     顺带去掉了聚焦时的蓝色发光（那是上一代设计语言的残留）。

  ── 三、「收入 600」和「收入 3427」────────────

  ★ 两个数现在都有名字了
     你的账没错，程序也没算错 —— 错的是**「收入」这个词被用在了两个数上**：
       汇总页写着 总收入 ¥3,427.44
       同一屏的收入饼图，加起来是 ¥600.51
       AI 分析拿到的是 3,427，于是它会说「你赚了三千四」

     现在口径统一成**含期初余额**（这样才和你 Excel 的「收入合计」对得上），
     但话说明白：
       总收入    ¥3,427.44   副标题：含期初余额 ¥2,826.93 · 和 Excel「收入合计」对得上
       其中真正挣到 ¥600.51   副标题：其余 ¥2,826.93 是开学时手里原有的钱
       结余      ¥1,828.29   副标题：总收入 − 支出
     收入饼图里「结余」**单独占一类**（不并进「其他收入」，否则一块占 99%），
     并注明「是期初余额，不是挣来的」。

     ⚠ 只有一处例外：**每月结余预估**仍然剔除期初。拿本金当收入去估
     「我每月能剩多少」，会算出一个永远达不到的数，那比少算一次收入危险。

  ★ 小数点后面那一长串
     账单日历的格子里印着 33.75000000000001 —— 浮点累加的老毛病。
     在**源头**收成两位（后端算完就 round），显示的地方再加一道兜底。
     凡是走 ¥ 格式的地方本来就有两位小数挡着，漏网的是不走 ¥ 格式的那几处。

  ── 四、图表颜色 ──

  ★ 色板重做
     以前是「主色 + 黄金角旋转」算出来的：能保证相邻两项分得开，**但保证不了
     取到的是好看的颜色** —— 换一套配色就换一组脏色。实测「蓝粉」下餐饮是
     洋红、医疗是橄榄绿、购物是蓝，一张饼上同时出现橄榄绿和土黄。

     现在色相是**固定的 11 个**，刻意跳过 25°~75° 那段黄绿 / 土橙；
     顺序也打散过，相邻两项至少差 122°。只让浓淡跟着主题走。
     另外：「其他」用中性灰（它是兜底桶，不该跟真类别抢注意力），
     深色主题下扇区的填充更浓（淡填充压在暗底上等于没有）。

  ── 五、细节 ──

  ★ 数字排队了（等宽数字）
     以前 1 比 8 窄、7 比 0 窄，一列金额、一列完成率全对不齐，
     看着就是"没排过版"。现在数字占同样宽度，自动成一条竖线。

  ★ 图标和数字的颜色跟着主题走了
     有好几处颜色是写死的十六进制（比如团购那四张卡里的 `#1f2933`），
     深色主题下就是深色字压深色卡 —— 看不见。全部改成主题变量。

  ★ 压在照片上的字能看清了
     页头那行说明、区块标题、页脚是直接压在照片上的，用最淡的那档灰
     只有 3:1 的对比。现在提到深一档 + 配一层跟主题相反的光晕。

  ★ 物资表的按钮不再"若有若无"
     「AI」「开封后多久」那些按钮原来是 30% 不透明 —— 27 行的表，
     全亮着确实太闹，但 30% 在深色卡片上基本等于不存在。提到 50%。

  ★ 接口不再被浏览器缓存
     这一版调样式时踩到的：改了设置，页面死活还是旧的，换一个干净的浏览器
     配置立刻就对。查出来是**所有接口的响应一个缓存头都没有**（静态文件那边
     是写了的）。接口现在一律 `no-store`。


---

## 2.3.6 改了什么


  这一版是最大的一次更新。分五块说。

  ── 一、长得不一样了 ──

  ★ 配色系统整个重做
     以前七套配色是分七次手调的，同一个「红」在不同配色里深浅都不一样，
     浅色和深色也不是一起设计的。现在改成：
       · 每套配色用**同一个公式**算出来（12 级色阶，借 Radix Colors 的做法）
       · 用 **OKLCH** 色空间 —— 它和人眼感觉一致，所以「深一档」在七套里
         真的是同样一档
       · **对比度是算出来的**，不是凭眼睛看的：正文压在底色上必定过标准
     「米黄」改名**素纸**：底色改成中性微冷（原来是暖米），强调色从陶土褐
     换成**朱砂红**。原来那套暖米 + 陶土正好撞在 AI 生成设计最典型的套路上。
     旧名字仍然认，老设置不会掉色。

  ★ 不再雾蒙蒙
     · 那层蒙版从 35% 一路降到 **10%**（只压照片，不再压卡片）
     · 卡片**通透拉到底也留 55% 不透明**，永远看得出边界
     · 通透时卡片**保留一根描边**，颜色跟着主题算（浅色出深灰、深色出浅灰）
     · 浅色主题下那圈把标题糊出毛边的白光晕，收到 1px

  ★ 随手记的输入框看不见字（bug）
     它的字色写死成白色，是给早期深色横幅配的 —— 后来横幅改成浅底深字了，
     这一块没跟着改，于是**浅色主题下白字压白卡**。你一直用深色所以没发现。

  ── 二、数据放哪，你说了算 ──

  ★ 设置 →「数据放在哪」
     · 显示当前目录，一键打开
     · **换个地方**：会先问「用安装目录，还是自己选一个」
     · 搬的时候是「**先复制、逐项核对、核对过了才动旧的**」——
       核对不通过，原数据一个字节都不删。旧目录默认保留。
     · 浏览器版换不了（数据位置是启动时定的），界面上会说清楚

  ★ 导出之后能直接打开所在文件夹

  ── 三、写日记这件事 ──

  ★ **15 套日记模板**，总览页「用模板写」一点就展开
     分四类，从「随手就能写」到「成套的方法」：
       感恩·心态  321 日记法 / 三件好事+原因 / 今日小确幸
       复盘·改进  KPT / YWT / 斯多葛晚间三问
       表达·情绪  结构化表达 / STAR / CBT 情绪日记 / PERMA
       成套方法   五分钟日记 / Gibbs 反思循环
       自由书写   晨间三页 / 自由书写 / 一句话日记
     每一栏都有引导语和例子，填几栏都行，空着的不写进去。
     写完拼成有结构的一段，**追加进今天的日报** —— 回忆书、周报月报都能搜到。

  ★ **随笔**（新的标签页）
     想到什么写什么。跟日记**同一个目录、但两个文件不混**。
     按**时刻**命名，一天可以写好几篇，不会互相覆盖。

  ★ **摘抄**
     一句话 + 出处 + 标签 + 「我为什么记它」。能按标签筛、能搜。
     正文用楷体排，看着像抄在本子上的。

  ★ **插图**
     日记和随笔的编辑器里都有「插图」按钮。图会缩到 1600px 存进
     `笔记\_附件\`，正文里插一段 Markdown。同一张图传两次只存一份。
     换数据目录时图跟着笔记一起走。

  ★ **导出成文档**（Word / Markdown / HTML）
     · Word：能改、能发给别人，图片内嵌进去了
     · Markdown：原样留底，记事本就能开
     · HTML：打开后 Ctrl+P 就能存成 PDF

  ── 四、看数据 ──

  ★ 打卡页下面多了「回头看」
     · **连续打卡天数** —— 今天还没打卡不算断（不然每天早上打开都是"已断"）
     · **这一年**：一格一天的热力图，一年记了多少天一眼看到
     · **最近 30 天**：睡眠、心情、精力三条线
     · **积分与奖励**：打一个勾算 1 分，全勤那天额外 +5；
       自己定义奖励（攒够 50 分买杯奶茶），攒够了点兑换

  ★ 账单页多了三块
     · **这个月花了多少**：一个月一屏，格子里是当天花的钱，越深花得越多
     · **订阅与定期扣款**：每月固定要扣的（话费、会员、房租），
       自动算每月合计、每年多少、下次哪天扣
     · **常用模板**：食堂 15、公交 2 这种常记的，存下来点一下就填好表单

  ★ 预算结转（设置 →「顺手的小开关」）
     上个月没花完的，加到下个月。**只结转正的**：上个月超支了不倒扣下个月
     —— 那会让一次超支连着惩罚后面好几个月。

  ★ 牛马时钟（设置 → 行为）
     填个日薪和每天干几小时，总览页显示「今天已经赚了多少」。自嘲用。

  ★ 全局快捷键（设置 → 行为）
     填 `CommandOrControl+Alt+X` 这种，在别的程序里按一下就把窗口叫出来。
     改完要重启才生效。

  ★ 壁纸三模式：照片 / 纯色 / 不要
     选照片时还能选「铺满 / 完整显示 / 居中」——
     图的比例和屏幕对不上时，铺满会裁掉一点，完整显示会留边。

  ── 五、AI 省钱（这一版最实在的一处）──

  ★ **输入框补全**那一处，原来占了你全部 AI 花费的 **76%**
     实测 83 次调用一共 ¥0.47，其中 42 次是「打名字时的候选」，花了 ¥0.36。
     原因不是「本地没匹配才问 AI」那条（那个本来就对），而是
     **每敲一个字都问一次**：打「复方感冒灵」五个字就是 5 次调用，
     每次 1400 多 token，而五次问出来的候选几乎一样。
     现在加了三道闸：
       ① 一个字不问（「复」能猜出什么没有参考价值）
       ② **一次输入只问一次** —— 问过的这一串不再重复问
       ③ 问过的词进缓存，同一个词永远不问第二遍
     同样的输入，调用次数能降到原来的 **五分之一到十分之一**。

  ★ **高峰时段提示**（设置 → AI）
     你的用量里 **59% 花在高峰时段**（工作日 9:00–12:00、14:00–18:00，
     那两段单价翻倍）。设置页里会按你自己的账算给你看，
     并提醒：周报月报这类不急的活儿挪到晚上或周末跑。

  ★ 回忆书的翻找力度可以调了
     单轮最多翻几次、一条给多少字、一轮总共读多少 ——
     调小更快更省，调大看到更多材料。默认值也调低了一档。

  ★ 提示词可以自己改（设置 → AI）
     日报整理 / 周报 / 月报 / 回忆书回答，四段都能改。
     **清空就是恢复默认** —— 不用担心改坏了找不回来。


---

## 2.3.5 改了什么


  ★ 浅色主题不再「全屏雾蒙蒙」
    是**三层浅色叠在一起**了。拿你当时那套设置（蓝粉 · 浅色 · 通透 75%）实算：
      · 照片上那层遮罩 35% —— 遮罩用的是主题底色，浅色主题下它是**白色**的，
        等于往照片上蒙了一层纱
      · 卡片只剩 25% 不透明（通透拉到了 75%）
      · 卡片背后还糊着 16px 的毛玻璃，卡片描边又被设成了透明
    三样一叠，整屏就只剩两三种几乎一样的浅色 —— 所以看着是「雾」，不是「透」。

    现在分三处改：
      · **卡片再怎么透也留 55% 不透明**，拉到底卡片还有轮廓。
        0~100 整段映射到 100%~55%，所以不会出现「后面一段拖了没反应」
      · **毛玻璃 16px → 6px**：是磨砂，不是乳白。照片的构图和轮廓还认得出
      · **遮罩默认 35% → 20%**：选图时自动估的那个值也一并变轻
        （以前一张偏亮的照片估出来正好 35，一上来就把照片洗白了）

  ★ 「压暗」这个滑块，在浅色下改叫「衬底」
    它压的是**主题底色**：深色主题下确实是压暗照片，浅色主题下是给照片垫一层
    浅底 —— 越拖越白。名字却一直叫「压暗」，说不通。
    现在跟着主题走：浅色下「衬底」，深色下「压暗」，提示文字也分开了。

  ★ 老用户存着的那个 35% 替你挪了一次
    估算公式改轻了，但**已经存下来的 35 不会自己变**。所以启动时会做一次修补：
    只把「正好等于旧默认 35」的值挪到 20，跑一次就记下；
    你自己拖过的别的值一律不碰，手动拖回 35 之后也不会再被降下去。


---

## 2.3.4 改了什么


  这一版只修 bug，没有新东西。

  ★ 修「换一张」点了没反应（紧挨着的「去掉背景」也一样）
    从**标签页**进设置的时候，程序里那份「渲染完要绑哪些事件」的清单
    是手抄的，抄漏了背景图那几个控件 —— 按钮画出来了、事件没绑上，
    点上去毫无反应，而且**不报任何错**。
    现在改成只认一份清单（afterRenderSettings），
    以后再加设置项，在那一处登记过就不会再漏。

  ★ 修「insertBefore 报错」的另一半
    2.3.3 修的是「先把要插入位置的那一行删了，回头再问它在哪儿」——
    只修了一半。改完拿到的锚点取的是**那一串里的第一个**，
    而紧跟它后面的兄弟（下一组标题、加一项按钮）**也要一起删** ——
    于是锚点还是被删掉了，报出来的是同一个错的另一个名字：
    `NotFoundError: ... is not a child of this node`。
    现在取那一串里**最后一个**的后面；按定义它不在删除名单里。
    改打卡项的名字、加一项、删一项都会走到这里，所以以前一碰就弹错。

  ★ 测试补了三条（这才是这两个错能溜出去的原因）
    · 点「换一张」到底有没有去开文件选择框 —— 以前测的是「直接给
      文件框塞一张图」，**绕过了按钮本身**，所以按钮死了照样全绿
    · 从标签页进设置之后按钮还活着 —— 以前测的是直接渲染设置页
    · 就地重画打卡项编辑器不报错、内容还在
    另外，测试页现在会把「脚本没能跑起来」直接写在结果区 ——
    以前内联脚本有语法错误只会停在一行 RUNNING，跟「跑得慢」长得一模一样。


---

## 2.3.3 改了什么


  ★ 四套新配色（偏女性向）
     樱粉 / 蓝粉 / 藕荷 / 奶茶，每套都有浅色和深色两版。
     设置页 →「外观」→ 配色，点一下当场换。
     原来那几套古风的都还在，随时能换回去。

  ★ 总览页摆什么、谁在前，你说了算
     设置页 →「个性化」→「总览页的卡片」：
       · 勾选  = 显示哪几张，不勾就不显示（**数据一条不少**）
       · ↑ ↓  = 谁排前面；也可以直接用鼠标拖
       · 一张都不勾 → 自动退回「全显示」（空白的总览页没意义）
     同一套操作也给「今日快速打卡」用了一遍：
     哪几个打卡项出现在总览页、按什么顺序排，都能自己定。
     ⚠ 顺手修了一个别扭的地方：以前把一张卡关掉再打开，
       它会掉到队伍最后 —— 现在关掉再打开还在原来那一格。

  ★ 资金类型可以自己加了
     账单里的「结余 / 账户」以前写死了微信、支付宝那几个。
     现在设置页 →「资金类型」能自己增删改：几张银行卡、几个钱包都行，
     记完账按账户一看，各放多少钱一目了然。

  ★ 修了三个毛病
     ① **待办点不动。** 绑事件那两行写错了标签页的 id，按钮全是死的
        —— 看得到、点不动、还不报错。
     ② **换背景要刷新才生效。** 换了图，浏览器手里那份旧图的缓存还被递回来，
        于是「选了新图，背景还是旧的那张」。现在每次换图带一个递增的版本号，
        而且明确告诉浏览器「这张图不要缓存」。
     ③ **偶尔弹「脚本错误：insertBefore of null」。** 重画「打卡项」列表时，
        先把它要插入位置的那一行删掉了，回头再问它「你在哪儿」——
        问的是已经被删掉的元素。改成先把位置记下来再删。


---

## 2.3.2 改了什么


  ★ 修了「别人电脑上装完打不开」
    两个错叠在一起，凑成了一句看不懂的报错。

    ① **主程序启动得太早。** `main()` 在 Electron 还没 ready 的时候就跑了，
       而决定「数据放哪儿」那一步要用系统弹框 —— 弹框在 ready 之前不能用，
       于是当场抛 `dialog module can only be used after app is ready`。
       现在改成等 ready 之后再启动。

    ② **报错的地方自己也炸了。** 上面那个错被 catch 住、要弹一个
       「启动失败」的框告诉你 —— 可那个框用的还是 dialog，还是不能用，
       于是又抛一次。最后你看到的是「启动失败 / 后端没能起来」加一堆无关的建议，
       真正的原因一个字都没露出来。
       现在报错函数自己**保证不抛异常**，而且会写明**卡在哪一步**
       （找数据文件夹 / 启动后端 / 创建窗口）。

  ★ 全新安装不再问「你的数据文件夹在哪」
    以前第一次打开会弹一个三选框：「选择数据文件夹 / 全新开始 / 退出」。
    对头一次用的人来说这个框根本没法回答 —— 他手里压根没有"数据文件夹"，
    点哪个都是猜。而且它还会报错（就是上面那两条），装完就是一脸懵。

    现在：**没得挑就不挑。** 头一次打开直接在「文档\小煦拾简」建一个
    属于自己的空数据库，不弹任何框。想换地方，改这一行就行：
        %APPDATA%\小煦拾简\数据位置.txt
    （免安装版还是老样子：把文件夹摆在数据旁边，它自己认得出来。）

  ★ 顺手修了一个「静默失败」
    记住数据目录的那一步，如果目标文件夹不存在，写文件会失败 ——
    而原来那里是 `catch {}` 一吞，什么痕迹都不留。表现是
    **每次开机都重新决定一遍数据目录**。现在会先建目录，写不成也会记进日志。


---

## 2.3.1 改了什么


  ★ 修了一个我自己搞出来的 bug：**点一下饼图，整张图就没了**
    2.3.0 换新配色时，颜色是用 `hsl(36 52% 46%)` 这种**空格分隔**写的。
    浏览器认这种写法，所以首次渲染看着一切正常 ——
    可 ECharts **内部**那个颜色解析器是按**逗号**切的，一点击高亮就走它，
    拿到的东西解析不出来，直接把颜色设成全透明。
    于是「点一下 → 图没了，只剩标签的引线还活着」。
    改回逗号就没事了。

  ★ 两个饼图改成**线条勾勒**
    扇区不再实心填色，改成 2px 描边勾出轮廓 + 里面一层很淡的同色（22%）。
    背景（照片 / 底色）能透上来，整张图轻了很多。

  ★ 结余卡片把收入的账说清了
    2.3.0 把「期初余额」从收入饼图里剔掉了，但界面上没说 ——
    于是「总收 ¥3,427」和「收入饼图合计 ¥600」对不上，看着像数据错了。
    现在结余卡下面写明白了：`期初 ¥2,826.93 ＋ 挣到 ¥600.51 － 支出 ¥1,599.15`。

  ★ 收入类别从 4 个拆成 10 个
    原来只有「生活费 / 兼职 / 奖学金 / 其他收入」，结果「其他收入」
    一项就占了九成多 —— 报销、返现、红包、卖闲置全挤在里面。
    现在多了：报销、返现、红包、二手、退款、理财。
    **已有的记录一条不动**，新类别只是让以后好归。

  ★ 几处图表的老毛病
    · y 轴数字不再被裁（以前字号调大后「3,500」会显示成「00」）
    · 饼图垂直居中（以前偏上，下面留白比上面多）
    · 宽屏下图表加高，不那么扁

  ★ 通透再进一步
    · 输入框、下拉菜单改成**只留边框、不填底色** ——
      以前铺一层实色，在玻璃卡片上就是一块块贴纸。
    · 「记一笔」这类主按钮的底色也带了一点透明（75%），
      但仍然保持实心彩色的辨识度。
    · 顺手把中性标签和进度条槽也接进了通透体系。


---

## 2.3 改了什么


  这一版一半是「好看」，一半是「改错」。改错那几条更要紧，先说。

  ★ 四个统计口径的错（都在数据上，不在画面上）
    ① 平均睡眠一直显示 0。换数据库那次，时间从「Excel 的小数」变成了
       文本 '01:49'，后端算时间的函数没跟上，解析失败被吞成空值。
       于是 AI 分析里说「睡眠数据全是 0，要么没填要么没睡」——
       而你明明填了。前端同一份数据算出来是对的，两边打架。
    ② 完成率的分母写死成 5。打卡项从 2.2 起能自己增删改了，
       加一个核心项 → 分子变 6、分母还是 5 → 完成率显示 120%。
       现在分子分母都从打卡项表里数。
    ③ 「本月完成率」把**还没到的日子**也算进了平均。月初打开一看
       「完成率 15%」，其实是被后面的空白日子稀释的。现在只算到今天。
    ④ 收入饼图把「结余」（期初余额）当成了收入。开学时手里本来就有的
       那 ¥2,826 被算进「收入」，占了八成多，真正挣到的那几百块被压没了。
       现在期初余额单独列出来，不进收入构成。

  ★ 背景：照片能真正透出来了
    · 新增「卡片通透」滑块（0–85%），另有 轻/中/透 三个预设。
      调上去之后卡片变成一层蒙着照片的毛玻璃，字靠一圈光晕托住。
    · 选图时会**自动量一下这张照片的明暗**，替你填好「压暗」的初值 ——
      照片越亮、跟主题差得越远，压得越多。你照样可以再拖。
    · 通透时卡片那圈硬边框会撤掉，改成柔和投影。
    · 状态标签和主操作按钮**保持实心彩色**，只有次要按钮跟着透 ——
      照片上「待核销」「付清并记账」这种还是得一眼认出来。

  ★ 统计图：配色跟着主题走了
    以前图表用的是 22 个写死的颜色（纯红纯蓝纯紫那套 Flat-UI 色），
    换主题、换深浅色它一动不动 —— 界面是纸墨调子，图表是塑料色。
    现在整组颜色从当前配色的主色推出来：米黄推赭金系、青瓷推青绿系，
    深色模式自动提亮。同一个类别在哪儿都是同一个颜色。
    另外：扇区之间留了缝、去掉重复的图例、太小的切片不标名字、
    「其他小额」改名「其余 N 项」（它以前跟真类别「其他」撞名）、
    柱子加了渐变和圆角、预算线从"一个孤零零的紫点"改成横贯的参考虚线、
    月份标签带上年份、没账的月份也画出来（趋势才成立）。

  ★ 窗口
    去掉了 Windows 那条原生标题栏 —— 它跟应用主题各管各的
    （应用深色 + 系统浅色时，会是一条白杠压在深绿页面上），
    还跟应用自己的顶栏重复。现在窗口按钮浮在顶栏右侧，只剩一层。
    顺手修了：开窗时的白闪、按住 Alt 会蹦出来的英文菜单。

  ★ 回忆书空状态
    整块垂直居中（以前底下 45% 是空的）、修了「基于」被拆行和
    源码换行漏成的多余空格、输入框限宽到 780px（以前一行 159 个汉字）、
    没配 API Key 时在这里就提示、空对话时不显示「开启新对话」。


---

## 2.2 改了什么


  ① 打卡项目能自己增删改了。⚙ 设置 → 个性化 → 打卡项：
     改名字、换类型（打勾/数字/时间/下拉/文本）、增一项、删一项、调顺序。
     分三组：核心（计入完成率）、加分、其它（只在单日页显示）。
     单日页和总览页的快速打卡会自动跟着变，不用管列号。
     · 内置项只能隐藏不能删（删了历史数据就没地方放了）；
       自己加的项才能真删。
     · 底部「还原默认」一键回到出厂那几项。

  ② 记账的类别增删改回来了（⚙ 设置 → 数据 → 账单类别）。

  ③ 「待办」独立成一个标签页了，物资页不再显示它。

  ④ AI 加了个「个人基本情况」（⚙ 设置 → AI）。你写几行关于自己的事 ——
     作息、身体、这学期在忙什么 —— 每次分析都会带上它，
     给的建议才贴你的实际，不像模板话。
     下面还有个开关「分析时也参考最近 3 天的日记」，**默认关**：
     日记是最私密的东西，该由你自己决定要不要喂给 AI。

  ⑤ 物资多了「开封日期」和「开封后(月)」两列。
     很多药和吃的，包装上的有效期是没开封的保质期 —— 眼药水开封一个月
     就不该再用了，哪怕瓶子上印着 2028 年。
     填上开封日期和月数之后，系统会**取两者中先到的那个**算到期日。
     不知道能放多久？点每行右边「开封后多久」，让 AI 按品类估一个，
     它会填进输入框，**你看过觉得不合适可以改，点了保存才生效**。

  ⑥ 背景能换成自己的照片了（⚙ 设置 → 外观 → 背景图）。
     选一张图，它会自动缩到 1600px 存在 _配置\背景\ 里，不上传任何地方。
     配两个滑块：压暗（照片亮的时候把字衬出来）和模糊（照片太花时用）。
     卡片和表格仍然是实心的 —— 字最多的地方得先保证看得清。

  ⑦ 图标全部换成简约线条风了，包括设置页左边那排分区按钮、
     待办页、以及各种弹窗标题。深色主题下也不再出现白底白字的提示条。

  ⑧ 导出修好了（以前点了没反应，是被一个返回值的小错憋住了）。


---

## 2.1 改了什么


  ① 看图录入能一次认多条了。以前一张账单列表截图只认回一条，
     现在逐条列出来给你核对：每行能改、能单条取消勾选，
     底部实时算「已勾选 N 条 · 支出合计 ¥X」。确认后逐条写进账本。

  ② 字号能真正调大了。以前那个滑杆只带动正文 —— 标题、表格、徽标
     全是写死的像素，拉到最大也没变。现在全站跟着一起缩放，上限也提到 22。

  ③ 修了一个记账的老毛病：核销状态下拉默认选中「待核销」，
     于是每一笔普通支出都被打上这个标，列表看着像全是团购。
     现在只有选「团购」时才需要核销状态，选「否」时那一栏是灰的。

  ④ 提示条在深色主题下是白字白底、一个字看不清，改成固定深底白字。

  ⑤ 设置页「总览页显示哪几张卡」的彩色表情图标，换成了和其它地方
     一致的线条图标。

  ⑥ 总览页那个「随手记」输入框以前像个贴上去的白盒子，
     改成无框、只留一条底线，跟横幅融在一起。


---

## 2.0 有什么不一样


  ① 数据搬家了：从三本 Excel 搬进了数据库（小煦拾简.db）。
     **第一次打开时会自动搬一次**，搬完逐项核对（九十多项），
     核对不过就拒绝启动、你的 Excel 一个字节都不会改。
     原来的三本 Excel 原地不动，另外**复制**一份到「_旧数据存档_日期」里。

     为什么要搬：以前改一个格都要把整本 Excel 重排一遍再落盘，慢；
     而且图片没地方放。换成数据库之后写入是毫秒级的，
     而且「付清尾款并记账」这种一步改两处的操作天然就是个事务。

  ② Excel 没被抛弃，降级成「导出格式」：
     ⚙ 设置 → 数据 → 导出成 Excel，随时能把全部数据吐回成三本表。
     内容和界面上看到的完全一致，汇总表的合计数是活公式。

  ③ 看图录入：账单页「从截图记账」、打卡页「从截图录睡眠」。
     拖张截图进去（或直接 Ctrl+V 粘贴），AI 认出来的东西会先摊开让你核对，
     **你点了确认才写进去**。账单和睡眠是长期数据，机器看错一位数要翻半天账，
     所以中间那步省不掉。

  ④ 应急信息能编辑了：⚙ 设置 → 应急。填完可以打印一份贴宿舍。

  ⑤ 自动备份改成备份整个数据库了，每天第一次改动前存一份。
     手动备份、恢复都在 ⚙ 设置 → 数据里，恢复前会自动把当前状态另存一份。


---
