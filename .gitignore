# ================================================================
# .gitignore —— 小煦拾简
# ----------------------------------------------------------------
# 这个文件是**隐私边界**，不是"整理习惯"。
# 这个项目的工作目录里混着**用户的真实生活数据**（记账明细、
# 日记、待办、体检记录…）。上传到公开仓库 = 把这些全泄露出去。
# **动这个文件之前先读下面每一段的理由。**
# ================================================================

# ---- 1 数据库：全是真实数据（已实测确认）----
# 实测：那几个测试根目录下的库各有几百行**真实记账明细**
# （消费备注、金额、日期、支付方式）。**一个字节都不能传。**
# 这里故意不举具体例子 —— 举例子就等于把真实数据的一个片段
# 写进了这个文件，而这个文件是要上传的。说清"里面是什么"，
# 说类别就够了，不要抄原文。
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ---- 2 测试根目录：里面全是真实数据的副本 ----
# 命名规矩：下划线开头的目录都是开发期的临时物（测试根、打包产物、脚本副本）。
_前端测试根/
_加密测试根/
_升级测试根/
_测试副本/
_复现根/

# ---- 3 Excel / 备份：可能含真实数据 ----
*.xlsx
*.xls
*备份*
*副本*

# ---- 4 打包产物：100+ MB 的二进制，不该进仓库 ----
_打包产物/
_打包产物_*/
_新包/
_build/dist/
_build/build/
dist/
build/
*.exe
*.blockmap
*.asar
*.AppImage
*.dmg

# ---- 5 依赖与构建缓存 ----
node_modules/
npm-cache/
.npm-cache/
caches/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.log
electron/build/
electron/node_modules/
electron/.npm-cache/

# ---- 6 浏览器 / Electron 的临时 profile ----
# 这些里面有 cookies、缓存、浏览记录
_pc*/
_tmpchrome*/
webview/

# ---- 7 用户自己的东西，跟项目无关 ----
# 组织部复试方案是个人材料，跟这个软件没关系
组织部复试方案*/
*复试方案*
用户需求和决策习惯.*
数据位置.txt

# ---- 8 开发期的临时脚本与探针 ----
_*.png
_*.log
_*.json
_*.txt
_*.html
_*.bak
_*.py
_*.js
_*.docx
_*.xlsx
_*.bundle

# ⚠ 测试脚本和开发工具**不进仓库**（用户要求：仓库只放源码和文档）。
#   这些东西留在本地用就行 —— 想在别处跑测试，从本地副本拷。

# ---- 9 密钥 ----
*.key
*.pem
.env
.env.*
secrets.json
# 离线备份包（10MB 二进制，源码已在仓库里，不用再存一份）
*.bundle

# ---- 10 文档是要进仓库的 ----
!docs/
!docs/**
!README.md
