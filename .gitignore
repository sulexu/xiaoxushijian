# ================================================================
# .gitignore —— 小煦拾简
# ----------------------------------------------------------------
# ⚠⚠ 这个文件是**隐私边界**，不是"整理习惯"。
#    这个项目的工作目录里混着**用户的真实生活数据**（记账明细、
#    日记、待办、体检记录…）。上传到公开仓库 = 把这些全泄露出去。
#    **动这个文件之前先读下面每一段的理由。**
# ================================================================

# ---- ① 数据库：全是真实数据（已实测确认）----
# 实测：_前端测试根/小煦拾简.db 有 617 行真实记账（"擂饭"、"deepseek充值"…），
#       _测试副本 701 行，_升级测试根 780 行。**一个字节都不能传。**
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# ---- ② 测试根目录：里面全是真实数据的副本 ----
# 命名规矩：`_` 开头的目录都是开发期的临时物（测试根、打包产物、脚本副本）。
# 下面逐个列出来，**不是为了整齐，是为了不清掉任何一个**。
_前端测试根/
_加密测试根/
_升级测试根/
_测试副本/
_复现根/

# ---- ③ Excel / 备份：可能含真实数据 ----
# _测试副本_改名前备份.xlsx 是从真实数据库导出来的
*.xlsx
*.xls
*备份*
*副本*

# ---- ④ 打包产物：100+ MB 的二进制，不该进仓库 ----
_打包产物/
_打包产物_*/
_build/dist/
_build/build/
_build/*.spec.bak
dist/
build/
*.exe
*.blockmap
*.AppImage
*.dmg

# ---- ⑤ 依赖与构建缓存 ----
node_modules/
npm-cache/
.npm-cache/
.cache./
caches/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.log
electron/build/
electron/node_modules/
electron/.npm-cache/

# ---- ⑥ 浏览器 / Electron 的临时 profile ----
# _pcg / _tmpchrome / _pcm… 这些里面有 cookies、缓存、浏览记录
_pcg/
_tmpchrome*/
_pc*/
_pf*/
_pb*/
_pd*/
_pj*/
_pk*/
_pm*/
_pn*/
_po*/
_pp*/
_pq*/
_pr*/
_ps*/
_pt*/
_pu*/
_pv*/
_pw*/
_px*/
_py*/
_pz*/
_pa*/
_pe*/
_pg*/
_ph*/
_pi*/
_pl*/
webview/

# ---- ⑦ 用户自己的东西，跟项目无关 ----
# 组织部复试方案是你的个人材料，跟这个软件没关系。
组织部复试方案*/
*复试方案*
用户需求和决策习惯.*
数据位置.txt

# ---- ⑧ 开发期的临时脚本与探针 ----
# `_` 开头的散件（截图、临时日志、一次性脚本）
_*.png
_*.log
_*.json
_*.txt
_*.html
_*.bak
_*.py
_*.js
_*.docx
_*.xlsx

# ⚠ 但下面这些 `_` 开头的**是要进仓库的**（测试与开发工具），
#   所以放在最后用 `!` 排除掉上面的通配。
!_自测.py
!_自测_notes.py
!_自测_加密.py
!_自测_迁移.py
!_升级校验.py
!_检查测试台.js
!_函数清单.js
!_函数清单_基线.txt
!_前端源码.js
!_验证拆分无损.js
!_验证安全链接.js
!_验证待办筛选.js
!_验证备份目录提示.js
!_验证支出环比.js
!_验证学期汇总.js
!_md转word.py
!_2.3打包校验.py

# ---- ⑨ 密钥（万一以后有人加）----
*.key
*.pem
.env
.env.*
secrets.json

# ---- �� �����Ĺ�����Ƭ ----
_�°�/
*.asar
